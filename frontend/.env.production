# Production Environment Variables
# 生产环境配置（通过FRP公网访问）

# 不设置VITE_WS_URL，让前端自动使用当前页面的host
# 这样无论是通过localhost还是公网IP访问，WebSocket都会使用相同的地址

# 如果你需要强制指定WebSocket地址，可以取消下面的注释：
# VITE_WS_URL=ws://47.108.221.231:60001

# App Title
VITE_APP_TITLE=BTC Watcher

# Environment
NODE_ENV=production
