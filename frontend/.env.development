# Vite Development Environment Variables
# 开发环境配置

# API Base URL (Vite会自动代理到这个地址)
VITE_API_BASE_URL=http://localhost:8000

# API URL for upload components (空字符串表示使用相对路径，通过Vite代理)
VITE_API_URL=

# WebSocket URL
# 开发环境下注释掉，让它自动使用当前页面的host
# 这样WebSocket会通过Vite dev server代理到后端
# VITE_WS_URL=ws://localhost:8000

# App Title
VITE_APP_TITLE=BTC Watcher

# Environment
NODE_ENV=development
